let EthPriceOracle = artifacts.require("EthPriceOracle")
let STBL = artifacts.require("STBL")
let Bank = artifacts.require("Bank")

module.exports = function(deployer) {
	/*deployer.deploy(EthPriceOracle).then(() =>
		deployer.deploy(STBL).then(() =>
			deployer.deploy(Bank)
		)
	)*/
}